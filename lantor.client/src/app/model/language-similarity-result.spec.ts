import { LanguageSimilarityResult } from './language-similarity-result';

describe('LanguageSimilarityResult', () => {
  it('should create an instance', () => {
    expect(new LanguageSimilarityResult()).toBeTruthy();
  });
});
