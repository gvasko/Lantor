import { MultilingualSample } from './multilingual-sample';

describe('MultilingualSample', () => {
  it('should create an instance', () => {
    expect(new MultilingualSample()).toBeTruthy();
  });
});
