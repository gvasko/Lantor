import { LanguageDetectorRequest } from './language-detector-request';

describe('LanguageDetectorRequest', () => {
  it('should create an instance', () => {
    expect(new LanguageDetectorRequest()).toBeTruthy();
  });
});
